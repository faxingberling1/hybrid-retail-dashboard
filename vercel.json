{
  "version": 2,
  "framework": null,
  "builds": [
    {
      "src": "apps/dashboard/package.json",
      "use": "@vercel/next",
      "config": {
        "distDir": ".next"
      }
    }
  ],
  "installCommand": "cd apps/dashboard && npm install",
  "buildCommand": "cd apps/dashboard && npx prisma generate && next build"
}