{
  "version": 2,
  "buildCommand": "cd apps/dashboard && npm install && npx prisma generate && next build",
  "installCommand": "",
  "outputDirectory": "apps/dashboard/.next",
  "framework": "nextjs",
  "regions": [
    "pdx1"
  ]
}