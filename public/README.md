# Public Assets

This directory contains static assets that are served directly by Next.js.

Files in this directory are accessible from the root URL. For example, `public/logo.png` can be accessed at `/logo.png`.
